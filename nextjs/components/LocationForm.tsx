'use client'
import { FormEvent, useState } from 'react'
export default function LocationForm() { const [message, setMessage] = useState(''); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const response = await fetch('/api/admin/lokasi', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); const data = await response.json(); setMessage(data.error || 'Lokasi berhasil ditambahkan.'); if (response.ok) window.location.reload() } return <form className="panel stack compact-form" onSubmit={submit}><p className="eyebrow">TAMBAH LOKASI</p><h2>Lokasi baru</h2><input name="namaLokasi" placeholder="Nama lokasi" required /><textarea name="alamat" placeholder="Alamat lengkap" rows={2} required /><div className="form-pair"><input name="latitude" type="number" step="any" placeholder="Latitude" required /><input name="longitude" type="number" step="any" placeholder="Longitude" required /></div><input name="radiusMeter" type="number" min="1" max="10000" defaultValue="100" placeholder="Radius (meter)" required /><div className="form-pair"><label>Masuk<input name="jamMasuk" type="time" defaultValue="08:00" required /></label><label>Pulang<input name="jamPulang" type="time" defaultValue="16:00" required /></label></div><label>Batas terlambat<input name="batasTerlambat" type="time" defaultValue="08:15" required /></label><select name="status" defaultValue="aktif"><option value="aktif">Aktif</option><option value="nonaktif">Nonaktif</option></select>{message && <div className="alert error">{message}</div>}<button>Simpan lokasi</button></form> }