'use client'

import { FormEvent, useState } from 'react'
export default function RequestForm() { const [message, setMessage] = useState(''); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const body = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch('/api/pengajuan', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const data = await response.json(); setMessage(data.message || data.error); if (response.ok) event.currentTarget.reset() } return <form className="panel stack" onSubmit={submit}><p className="eyebrow">BUAT PENGAJUAN</p><h2>Ketidakhadiran</h2><label>Jenis<select name="jenis" defaultValue="izin"><option value="izin">Izin</option><option value="sakit">Sakit</option><option value="cuti">Cuti</option></select></label><label>Kategori alasan<input name="kategoriAlasan" placeholder="Urusan keluarga" required /></label><label>Tanggal mulai<input name="tanggalMulai" type="date" required /></label><label>Tanggal selesai<input name="tanggalSelesai" type="date" required /></label><label>Alasan<textarea name="alasan" rows={4} required /></label>{message && <div className="alert error">{message}</div>}<button>Kirim pengajuan</button></form> }