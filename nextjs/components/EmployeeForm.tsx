'use client'
import { FormEvent, useState } from 'react'
export default function EmployeeForm({ positions }: { positions: { id: number; nama: string }[] }) { const [message, setMessage] = useState(''); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const response = await fetch('/api/admin/pegawai', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); const data = await response.json(); setMessage(data.error || 'Karyawan berhasil ditambahkan.'); if (response.ok) { event.currentTarget.reset(); window.location.reload() } } return <form className="panel stack compact-form" onSubmit={submit}><p className="eyebrow">TAMBAH DATA</p><h2>Karyawan baru</h2><input name="nomorPegawai" placeholder="Nomor pegawai" required /><input name="nama" placeholder="Nama lengkap" required /><select name="jabatanId" defaultValue=""><option value="">Pilih jabatan</option>{positions.map((item) => <option key={item.id} value={item.id}>{item.nama}</option>)}</select><select name="jenisKelamin" defaultValue="L"><option value="L">Laki-laki</option><option value="P">Perempuan</option></select><input name="email" type="email" placeholder="Email (opsional)" /><input name="username" placeholder="Username login" required /><input name="password" type="password" placeholder="Password minimal 6 karakter" minLength={6} required /><textarea name="alamat" placeholder="Alamat" rows={3} />{message && <div className="alert error">{message}</div>}<button>Tambah karyawan</button></form> }