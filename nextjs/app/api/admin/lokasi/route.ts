import { NextResponse } from 'next/server'
import { getSession } from '@/lib/auth'
import { db } from '@/lib/db'

export async function GET() { const session = await getSession(); if (session?.role !== 'admin') return NextResponse.json({ error: 'Akses ditolak.' }, { status: 403 }); return NextResponse.json(await db.lokasiPresensi.findMany({ orderBy: { createdAt: 'desc' } })) }
export async function POST(request: Request) { const session = await getSession(); if (session?.role !== 'admin') return NextResponse.json({ error: 'Akses ditolak.' }, { status: 403 }); const body = await request.json(); if (body.status === 'aktif') await db.lokasiPresensi.updateMany({ data: { status: 'nonaktif' } }); const item = await db.lokasiPresensi.create({ data: { namaLokasi: body.namaLokasi, alamat: body.alamat, latitude: body.latitude, longitude: body.longitude, radiusMeter: Number(body.radiusMeter), jamMasuk: body.jamMasuk, batasTerlambat: body.batasTerlambat, jamPulang: body.jamPulang, status: body.status } }); return NextResponse.json(item, { status: 201 }) }