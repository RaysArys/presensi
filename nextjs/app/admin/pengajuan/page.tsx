import { redirect } from 'next/navigation'
import { getSession } from '@/lib/auth'
import { db } from '@/lib/db'
import AppShell from '@/components/AppShell'
import ApprovalButtons from '@/components/ApprovalButtons'
export default async function AdminRequestsPage() { const session = await getSession(); if (session?.role !== 'admin') redirect('/login'); const requests = await db.ketidakhadiran.findMany({ include: { pegawai: true }, orderBy: { createdAt: 'desc' } }); return <AppShell><main className="dashboard"><header className="topbar"><div><p className="eyebrow">ADMIN / PENGAJUAN</p><h1>Cuti & Izin</h1><p className="muted">Tinjau dan proses pengajuan ketidakhadiran.</p></div></header><article className="panel table-panel"><div className="table-scroll"><table><thead><tr><th>Karyawan</th><th>Jenis</th><th>Periode</th><th>Alasan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{requests.map((item) => <tr key={item.id}><td><strong>{item.pegawai.nama}</strong><small>{item.pegawai.nomorPegawai}</small></td><td>{item.jenis}<small>{item.kategoriAlasan}</small></td><td>{item.tanggalMulai.toLocaleDateString('id-ID')}<br />{item.tanggalSelesai.toLocaleDateString('id-ID')}</td><td>{item.alasan}</td><td><span className="pill pending">{item.statusPengajuan}</span></td><td>{item.statusPengajuan === 'menunggu' ? <ApprovalButtons id={item.id} /> : 'Sudah diproses'}</td></tr>)}</tbody></table></div></article></main></AppShell> }